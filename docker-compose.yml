services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/challenge.db
    volumes:
      - challenge-data:/data

volumes:
  challenge-data:
